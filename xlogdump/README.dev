
Build Test Instruction
======================

 1. Create `/tmp/pgsql/source' and extract the postgresql source tarballs
    in it.

 2. Check `VERSIONS' variable in `tests/build_test.sh'.

 3. Run `./test/build_test.sh' (in the xlogdump source root).

 4. `xlogdump-${VER}' executable(s) must be created.

